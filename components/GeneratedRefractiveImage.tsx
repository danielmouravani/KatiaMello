import React from 'react';

// This component is no longer used as we switched to static images for better performance.
// Kept as a stub to prevent import errors if referenced anywhere.
const GeneratedRefractiveImage: React.FC<any> = () => {
  return null;
};

export default GeneratedRefractiveImage;